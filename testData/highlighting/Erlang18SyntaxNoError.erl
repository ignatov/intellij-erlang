-module('Erlang18SyntaxNoError').

-optional_callbacks([]).

<warning descr="Unused function 'minimum_player_check/1'">minimum_player_check</warning>(<warning descr="Unused variable 'Players'">Players</warning>) ->
  maybe(1).

maybe(_) -> ok.